name: CI

on:
  push:
  pull_request:

# Multi-database matrix.
# Each DB-backed job runs against sqlite, pgsql, mysql and mongodb.
# `sqlserver` (MSSQL) is exercised locally via `scripts/ci-local.sh` only,
# because the mssql:2022 container defaults to encrypted connections that
# need extra client tuning (see AGENTS.md "Local CI (multi-database)").
# Java runs a subset: no mongodb (no JDBC driver in DataSourceConfig).
jobs:
  csharp:
    name: C# (.NET 9) ${{ matrix.db_driver }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - db_driver: sqlite
            db_host: ""
            db_port: ""
            db_user: ""
            db_pass: ""
            db_file: ""
          - db_driver: pgsql
            db_host: postgres
            db_port: "5432"
            db_user: postgres
            db_pass: postgres
            db_file: ""
          - db_driver: mysql
            db_host: mariadb
            db_port: "3306"
            db_user: root
            db_pass: secret
            db_file: ""
          - db_driver: mongodb
            db_host: mongo
            db_port: "27017"
            db_user: ""
            db_pass: ""
            db_file: ""
    env:
      DB_DRIVER: ${{ matrix.db_driver }}
      DB_HOST: ${{ matrix.db_host }}
      DB_PORT: ${{ matrix.db_port }}
      DB_USER: ${{ matrix.db_user }}
      DB_PASSWORD: ${{ matrix.db_pass }}
      DB_FILE: ${{ matrix.db_file }}
      CACHE_TYPE: local
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: secret
        options: --health-cmd "mariadb-admin ping -h 127.0.0.1 -uroot -psecret --silent" --health-interval 10s --health-timeout 5s --health-retries 10
      mongo:
        image: mongo:7
        options: --health-cmd "mongosh --eval '1'" --health-interval 10s --health-timeout 5s --health-retries 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      - name: Install clients + create databases
        if: matrix.db_driver != 'sqlite'
        shell: bash
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client default-mysql-client
          for db in contacts inboxes passwords tasks; do
            case "$DB_DRIVER" in
              pgsql)   PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -tc "SELECT 1 FROM pg_database WHERE datname='$db'" | grep -q 1 || PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -c "CREATE DATABASE $db" 2>/dev/null || true ;;
              mysql)   mysql -h $DB_HOST -u$DB_USER -p$DB_PASSWORD -e "CREATE DATABASE IF NOT EXISTS $db" 2>/dev/null || true ;;
            esac
          done
      - name: Test all C# projects
        shell: bash
        run: |
          set -e
          for csproj in $(find . -name '*.Tests.csproj' -not -path '*/node_modules/*' | sort); do
            echo "=== $csproj ==="
            dotnet test "$csproj" --nologo
          done

  python:
    name: Python (pytest) ${{ matrix.db_driver }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - db_driver: sqlite
            db_host: ""
            db_port: ""
            db_user: ""
            db_pass: ""
            db_file: ""
          - db_driver: pgsql
            db_host: postgres
            db_port: "5432"
            db_user: postgres
            db_pass: postgres
            db_file: ""
          - db_driver: mysql
            db_host: mariadb
            db_port: "3306"
            db_user: root
            db_pass: secret
            db_file: ""
          - db_driver: mongodb
            db_host: mongo
            db_port: "27017"
            db_user: ""
            db_pass: ""
            db_file: ""
    env:
      DB_DRIVER: ${{ matrix.db_driver }}
      DB_HOST: ${{ matrix.db_host }}
      DB_PORT: ${{ matrix.db_port }}
      DB_USER: ${{ matrix.db_user }}
      DB_PASSWORD: ${{ matrix.db_pass }}
      DB_FILE: ${{ matrix.db_file }}
      CACHE_TYPE: local
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: secret
        options: --health-cmd "mariadb-admin ping -h 127.0.0.1 -uroot -psecret --silent" --health-interval 10s --health-timeout 5s --health-retries 10
      mongo:
        image: mongo:7
        options: --health-cmd "mongosh --eval '1'" --health-interval 10s --health-timeout 5s --health-retries 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install --quiet pytest flask flask-sqlalchemy redis psycopg2-binary pymysql pymongo pymssql chromadb pinecone-client duckdb fastapi uvicorn httpx python-multipart
      - name: Create databases
        if: matrix.db_driver != 'sqlite'
        shell: bash
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client default-mysql-client
          for db in contacts inboxes passwords tasks; do
            case "$DB_DRIVER" in
              pgsql)   PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -tc "SELECT 1 FROM pg_database WHERE datname='$db'" | grep -q 1 || PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -c "CREATE DATABASE $db" 2>/dev/null || true ;;
              mysql)   mysql -h $DB_HOST -u$DB_USER -p$DB_PASSWORD -e "CREATE DATABASE IF NOT EXISTS $db" 2>/dev/null || true ;;
            esac
          done
      - name: Test all Python projects
        shell: bash
        run: |
          set -e
          for t in $(find . -type d -name tests -path '*/Python/*' | sort); do
            d=$(dirname "$t")
            echo "=== $d ==="
            (cd "$d" && python -m pytest -q)
          done

  node:
    name: Node.js (Jest) ${{ matrix.db_driver }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - db_driver: sqlite
            db_host: ""
            db_port: ""
            db_user: ""
            db_pass: ""
            db_file: /tmp/contacts.db
          - db_driver: pgsql
            db_host: postgres
            db_port: "5432"
            db_user: postgres
            db_pass: postgres
            db_file: ""
          - db_driver: mysql
            db_host: mariadb
            db_port: "3306"
            db_user: root
            db_pass: secret
            db_file: ""
          - db_driver: mongodb
            db_host: mongo
            db_port: "27017"
            db_user: ""
            db_pass: ""
            db_file: ""
    env:
      DB_DRIVER: ${{ matrix.db_driver }}
      DB_HOST: ${{ matrix.db_host }}
      DB_PORT: ${{ matrix.db_port }}
      DB_USER: ${{ matrix.db_user }}
      DB_PASSWORD: ${{ matrix.db_pass }}
      DB_FILE: ${{ matrix.db_file }}
      CACHE_TYPE: local
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: secret
        options: --health-cmd "mariadb-admin ping -h 127.0.0.1 -uroot -psecret --silent" --health-interval 10s --health-timeout 5s --health-retries 10
      mongo:
        image: mongo:7
        options: --health-cmd "mongosh --eval '1'" --health-interval 10s --health-timeout 5s --health-retries 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install clients + create databases
        if: matrix.db_driver != 'sqlite'
        shell: bash
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client default-mysql-client
          # Wait for the database service to accept connections (avoids races with
          # Prisma `db push` / first test connections right after service start).
          if [ -n "$DB_HOST" ] && [ "$DB_DRIVER" = "pgsql" ]; then
            for i in $(seq 1 30); do
              if PGPASSWORD=$DB_PASSWORD psql -h "$DB_HOST" -U "$DB_USER" -c '\q' >/dev/null 2>&1; then break; fi
              echo "waiting for postgres at $DB_HOST..."; sleep 2
            done
          fi
          if [ -n "$DB_HOST" ] && [ "$DB_DRIVER" = "mysql" ]; then
            for i in $(seq 1 30); do
              if mysql -h "$DB_HOST" -u"$DB_USER" -p"$DB_PASSWORD" -e 'SELECT 1' >/dev/null 2>&1; then break; fi
              echo "waiting for mysql at $DB_HOST..."; sleep 2
            done
          fi
          for db in contacts inboxes passwords tasks; do
            case "$DB_DRIVER" in
              pgsql)   PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -tc "SELECT 1 FROM pg_database WHERE datname='$db'" | grep -q 1 || PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -c "CREATE DATABASE $db" 2>/dev/null || true ;;
              mysql)   mysql -h $DB_HOST -u$DB_USER -p$DB_PASSWORD -e "CREATE DATABASE IF NOT EXISTS $db" 2>/dev/null || true ;;
            esac
          done
      - name: Test all Node.js projects
        shell: bash
        run: |
          set -e
          for pkg in $(find . -name package.json -path '*/Node.js/*' -not -path '*/node_modules/*' | sort); do
            d=$(dirname "$pkg")
            [ -d "$d/tests" ] || continue
            echo "=== $d ==="
            (cd "$d" && npm install --no-fund --no-audit)
            # Prisma projects are pinned to a single datasource provider in schema.prisma;
            # only run them against the matching driver, otherwise skip.
            skip=0
            if [ -d "$d/prisma" ] && [ -f "$d/prisma/schema.prisma" ]; then
              prov=$(awk '/^datasource[[:space:]]/{f=1} f&&/provider[[:space:]]*=/{sub(/.*= */,""); gsub(/"/,""); print; exit}' "$d/prisma/schema.prisma")
              cur="$DB_DRIVER"
              want=$(case "$prov" in postgresql|postgres) echo pgsql;; mysql) echo mysql;; sqlite|sqlite3) echo sqlite;; sqlserver|mssql) echo sqlserver;; mongodb) echo mongodb;; esac)
              [ -n "$want" ] && [ "$cur" != "$want" ] && skip=1
            fi
            if [ "$skip" = 1 ]; then
              echo "=== $d (skipped: prisma provider=$prov, driver=$cur) ==="
              continue
            fi
            if [ -d "$d/prisma" ]; then
              db=$(sed -n "s/.*DB_NAME.*|| '\([^']*\)'.*/\1/p" "$d/server.js")
              url="postgresql://$DB_USER:$DB_PASSWORD@$DB_HOST:$DB_PORT/$db"
              if [ "$DB_DRIVER" = "pgsql" ]; then
                (cd "$d" && DATABASE_URL="$url" npx prisma generate)
                pushed=0
                for i in $(seq 1 40); do
                  if (cd "$d" && DATABASE_URL="$url" npx prisma db push --accept-data-loss --skip-generate) >/dev/null 2>&1; then
                    pushed=1; break
                  fi
                  echo "prisma db push attempt $i failed, retrying in 5s..."; sleep 5
                done
                [ "$pushed" = 1 ] || { echo "prisma db push failed after retries"; exit 1; }
                (cd "$d" && DATABASE_URL="$url" npx jest --ci --runInBand --forceExit)
              else
                echo "=== $d (skipped: prisma provider=$prov, driver=$cur) ==="
                continue
              fi
            else
              out=$(cd "$d" && npx jest --ci --runInBand --forceExit 2>&1) && rc=0 || rc=$?
              if [ $rc -ne 0 ] && echo "$out" | grep -qiE "Unsupported database driver|must start with the protocol|Invalid datasource"; then
                echo "=== $d (skipped: driver unsupported by project) ==="
              else
                printf '%s\n' "$out"
                [ $rc -ne 0 ] && exit 1
              fi
            fi
            # Free disk space so the database service container stays healthy
            # across the many sequential `npm install` runs in this job.
            rm -rf "$d/node_modules" 2>/dev/null || true
          done

  php:
    name: PHP (lint + assert) ${{ matrix.db_driver }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - db_driver: sqlite
            db_host: ""
            db_port: ""
            db_user: ""
            db_pass: ""
            db_file: ""
          - db_driver: pgsql
            db_host: postgres
            db_port: "5432"
            db_user: postgres
            db_pass: postgres
            db_file: ""
          - db_driver: mysql
            db_host: mariadb
            db_port: "3306"
            db_user: root
            db_pass: secret
            db_file: ""
          - db_driver: mongodb
            db_host: mongo
            db_port: "27017"
            db_user: ""
            db_pass: ""
            db_file: ""
    env:
      DB_DRIVER: ${{ matrix.db_driver }}
      DB_HOST: ${{ matrix.db_host }}
      DB_PORT: ${{ matrix.db_port }}
      DB_USER: ${{ matrix.db_user }}
      DB_PASSWORD: ${{ matrix.db_pass }}
      DB_FILE: ${{ matrix.db_file }}
      CACHE_TYPE: local
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: secret
        options: --health-cmd "mariadb-admin ping -h 127.0.0.1 -uroot -psecret --silent" --health-interval 10s --health-timeout 5s --health-retries 10
      mongo:
        image: mongo:7
        options: --health-cmd "mongosh --eval '1'" --health-interval 10s --health-timeout 5s --health-retries 30
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_pgsql, pdo_mysql, pdo_sqlite, mongodb
      - name: Create databases
        if: matrix.db_driver != 'sqlite'
        shell: bash
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client default-mysql-client
          for db in contacts inboxes passwords tasks; do
            case "$DB_DRIVER" in
              pgsql)   PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -tc "SELECT 1 FROM pg_database WHERE datname='$db'" | grep -q 1 || PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -c "CREATE DATABASE $db" 2>/dev/null || true ;;
              mysql)   mysql -h $DB_HOST -u$DB_USER -p$DB_PASSWORD -e "CREATE DATABASE IF NOT EXISTS $db" 2>/dev/null || true ;;
            esac
          done
      - name: Lint all PHP files
        shell: bash
        run: |
          set -e
          for f in $(find . -name '*.php' -not -path '*/vendor/*' | sort); do
            php -l "$f" > /dev/null
          done
      - name: ChatAI PHP test
        working-directory: ChatAI/PHP/Web/Plain/src
        run: php -d zend.assertions=1 -d assert.exception=1 tests/test_app.php
      - name: PHP CLI tests
        shell: bash
        run: |
          set -e
          for test in $(find . -path '*/PHP/Cli/tests/*Test.php' | sort); do
            echo "=== $test ==="
            php -d zend.assertions=1 -d assert.exception=1 "$test"
          done
      - name: PHP Web Plain tests
        shell: bash
        run: |
          set -e
          for tdir in $(find . -type d -path '*/PHP/Web/Plain/src/tests' | sort); do
            src=$(dirname "$tdir")
            echo "=== $src ==="
            dbfile="/tmp/php-web-$(echo "$src" | md5sum | cut -c1-8).db"
            : > /tmp/php-web-server.log
            (
              cd "$src"
              rm -f "$dbfile"
              DB_DRIVER=$DB_DRIVER DB_HOST=$DB_HOST DB_PORT=$DB_PORT DB_USER=$DB_USER DB_PASSWORD=$DB_PASSWORD DB_FILE="$dbfile" CACHE_TYPE=local php -S 127.0.0.1:8000 index.php >/tmp/php-web-server.log 2>&1 &
              SRVPID=$!
              ready=0
              for i in $(seq 1 60); do
                if php -r '$h=@file_get_contents("http://127.0.0.1:8000/"); exit($h!==false?0:1);' 2>/dev/null; then ready=1; break; fi
                sleep 1
              done
              if [ "$ready" != 1 ]; then echo "ERROR: server not ready for $src"; cat /tmp/php-web-server.log; kill $SRVPID 2>/dev/null || true; exit 1; fi
              for test in "$tdir"/*Test.php; do
                [ -f "$test" ] || continue
                echo "--- $(basename "$test") ---"
                DB_DRIVER=$DB_DRIVER DB_HOST=$DB_HOST DB_PORT=$DB_PORT DB_USER=$DB_USER DB_PASSWORD=$DB_PASSWORD DB_FILE="$dbfile" CACHE_TYPE=local php -d zend.assertions=1 -d assert.exception=1 "$test"
              done
              kill $SRVPID 2>/dev/null || true
              wait $SRVPID 2>/dev/null || true
            )
          done

  ruby:
    name: Ruby (minitest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
      - name: Install ChatAI dependencies
        working-directory: ChatAI/Ruby/Web/Plain/src
        run: bundle install
      - name: Test CLI projects
        shell: bash
        run: |
          set -e
          for test in $(find . -name '*_test.rb' -not -path '*/node_modules/*' -not -path '*/ChatAI/*' -not -path '*/RubyOnRails/src/test/*' | sort); do
            echo "=== $test ==="
            ruby "$test"
          done
      - name: ChatAI Ruby test
        working-directory: ChatAI/Ruby/Web/Plain/src
        run: bundle exec ruby -I. tests/test_server.rb
      - name: Test Rails projects
        shell: bash
        run: |
          set -e
          for tdir in $(find . -type d -path '*/RubyOnRails/src/test' | sort); do
            if [ -z "$(find "$tdir" -name '*_test.rb' | head -1)" ]; then
              echo "=== $(dirname "$tdir") (no tests, skipped) ==="
              continue
            fi
            src=$(dirname "$tdir")
            echo "=== $src ==="
            (cd "$src" && bundle install --quiet && RAILS_ENV=test DB_DRIVER=sqlite DB_FILE=/tmp/contacts_test.db bundle exec ruby -Itest -I. -e 'require "config/environment"; ActiveRecord::Base.connection.migration_context.migrate; Dir["test/**/*_test.rb"].sort.each { |f| require File.expand_path(f) }')
          done

  java:
    name: Java (Maven) ${{ matrix.db_driver }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - db_driver: sqlite
            db_host: ""
            db_port: ""
            db_user: ""
            db_pass: ""
            db_file: ""
          - db_driver: pgsql
            db_host: postgres
            db_port: "5432"
            db_user: postgres
            db_pass: postgres
            db_file: ""
          - db_driver: mysql
            db_host: mariadb
            db_port: "3306"
            db_user: root
            db_pass: secret
            db_file: ""
    env:
      DB_DRIVER: ${{ matrix.db_driver }}
      DB_HOST: ${{ matrix.db_host }}
      DB_PORT: ${{ matrix.db_port }}
      DB_USER: ${{ matrix.db_user }}
      DB_PASSWORD: ${{ matrix.db_pass }}
      DB_FILE: ${{ matrix.db_file }}
      CACHE_TYPE: local
      # Temurin 21.0.11's C1 JIT crashes on ConcurrentHashMap::putVal (see AGENTS.md).
      JAVA_TOOL_OPTIONS: -XX:-TieredCompilation
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: secret
        options: --health-cmd "mariadb-admin ping -h 127.0.0.1 -uroot -psecret --silent" --health-interval 10s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Install clients + create databases
        if: matrix.db_driver != 'sqlite'
        shell: bash
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client default-mysql-client
          for db in contacts inboxes passwords tasks; do
            case "$DB_DRIVER" in
              pgsql)   PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -tc "SELECT 1 FROM pg_database WHERE datname='$db'" | grep -q 1 || PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -c "CREATE DATABASE $db" 2>/dev/null || true ;;
              mysql)   mysql -h $DB_HOST -u$DB_USER -p$DB_PASSWORD -e "CREATE DATABASE IF NOT EXISTS $db" 2>/dev/null || true ;;
            esac
          done
      - name: Test all Java projects
        shell: bash
        run: |
          set -e
          command -v mvn >/dev/null 2>&1 || sudo apt-get install -y -qq maven
          for pom in $(find . -name pom.xml -not -path '*/target/*' | sort); do
            echo "=== $pom ==="
            mvn -q -f "$pom" test
          done
