# Shared CloudLocal Utilities

Shared scripts and healthchecks for local cloud emulators.

Keep reusable provider-neutral helpers here. Provider-specific setup should stay under `aws/`, `gcp/` or `azure/`.
